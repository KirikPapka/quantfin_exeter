# Copy to `.env` locally (gitignored). On Render, set variables in the dashboard.
# Point at the folder that contains `features/` (not the parquet files themselves).
CFA_DATA_ROOT=

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
